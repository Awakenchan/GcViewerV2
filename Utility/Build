return setmetatable({}, {
    __index = function(_, key)
        if key == "__version" then
            return "Alpha version 1.5"
        elseif key == "__log" then
            return {
                "[+] Added Filtergc Support",
                "[+] Rewrote Grab-Function",
                "[-] Rewrote gc loops and all for loops"
            }
        end
    end
})
