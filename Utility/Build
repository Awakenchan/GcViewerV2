return setmetatable({}, {
    __index = function(_, key)
        if key == "__version" then
            return "2.7"
        elseif key == "__log" then
            return {
                "[+] Fixed potassium issue",
                "[/] Added my crypto address if yall wana support!"
            }
        end
    end
})
