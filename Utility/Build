return setmetatable({}, {
    __index = function(_, key)
        if key == "__version" then
            return "2.7"
        elseif key == "__log" then
            return {
                "[+] Added actor support",
                "[-] Removed adonis finder"
            }
        end
    end
})
