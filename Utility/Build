return setmetatable({}, {
    __index = function(_, key)
        if key == "__version" then
            return "2.2"
        elseif key == "__log" then
            return {
                "[/] Currently rewriting getscript() log",
                "[/] Attempt to fix all issues",
                "[/] Attempt to decrease fail to decompile",
            }
        end
    end
})
